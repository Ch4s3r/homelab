apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - postgres-cluster.yaml
  - ingress.yaml

helmCharts:
  - name: preparr
    repo: oci://ghcr.io/robbeverhelst/charts
    # renovate: datasource=docker depName=ghcr.io/robbeverhelst/charts/preparr versioning=semver
    version: 0.19.5
    releaseName: preparr
    namespace: media
    valuesFile: values.yaml
