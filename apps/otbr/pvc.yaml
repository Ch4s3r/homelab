apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: otbr-data-pvc
  namespace: otbr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
